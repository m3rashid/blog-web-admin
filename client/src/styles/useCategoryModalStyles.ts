import { createStyles } from '@mantine/core'

export const useCategoryModalStyles = createStyles((theme) => ({
  input: {
    fontFamily: theme.fontFamily,
    input: {},
  },
  modal: {
    fontWeight: 700,
  },
}))
